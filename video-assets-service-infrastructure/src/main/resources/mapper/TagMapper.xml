<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.video.assets.infrastructure.mapper.TagMapper">

    <!-- 热门标签统计 -->
    <select id="selectTopTags" resultType="com.video.assets.domain.dto.TagCountDTO">
        SELECT t.name, COUNT(*) AS count
        FROM tags t
        JOIN asset_tags at ON t.id = at.tag_id
        GROUP BY t.id, t.name
        ORDER BY count DESC
        LIMIT #{limit}
    </select>

    <!-- 插入标签，若已存在则更新（实际不变），始终返回 ID -->
    <select id="insertOrIgnore" resultType="java.lang.Long">
        INSERT INTO tags (name) VALUES (#{name})
        ON CONFLICT (name) DO UPDATE SET name = EXCLUDED.name
        RETURNING id
    </select>

</mapper>
